
USE car_rental;

CREATE TABLE `users` (
  `id` int NOT NULL AUTO_INCREMENT,
  `full_name` varchar(100) DEFAULT NULL,
  `email` varchar(100) DEFAULT NULL,
  `phone` varchar(20) DEFAULT NULL,
  `role` enum('admin','employee','customer') NOT NULL,
  `password_hash` varchar(255) NOT NULL,
  `driving_license_no` varchar(50) DEFAULT NULL,
  `salary` decimal(10,2) DEFAULT NULL,
  PRIMARY KEY (`id`)
);
select * from users;
CREATE TABLE cars (
  id int NOT NULL AUTO_INCREMENT,
  brand varchar(50) DEFAULT NULL,
  model varchar(50) DEFAULT NULL,
  category varchar(50) DEFAULT NULL,
  year int DEFAULT NULL,
  status tinyint(1) DEFAULT '1',
  image_url varchar(255) DEFAULT NULL,
  PRIMARY KEY (id)
);


CREATE TABLE car_prices (
    id INT AUTO_INCREMENT PRIMARY KEY,
    car_id INT NOT NULL,
    day ENUM(
        'monday','tuesday','wednesday',
        'thursday','friday','saturday','sunday'
    ),
    price DECIMAL(10,2),
    FOREIGN KEY (car_id) REFERENCES cars(id)
        ON DELETE CASCADE
);

CREATE TABLE rentals (
    id INT AUTO_INCREMENT PRIMARY KEY,
    user_id INT NOT NULL,
    employee_id INT,
    car_id INT NOT NULL,
    pickup_location VARCHAR(100),
    dropoff_location VARCHAR(100),
    start_date DATETIME,
    end_date DATETIME,
    total_price DECIMAL(10,2),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,

    FOREIGN KEY (user_id) REFERENCES users(id),
    FOREIGN KEY (employee_id) REFERENCES users(id),
    FOREIGN KEY (car_id) REFERENCES cars(id)
);

CREATE TABLE payments (
    id INT AUTO_INCREMENT PRIMARY KEY,
    rental_id INT NOT NULL,
    user_id INT NOT NULL,
    amount DECIMAL(10,2),
    payment_method VARCHAR(50),
    payment_status VARCHAR(50),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,

    FOREIGN KEY (rental_id) REFERENCES rentals(id)
        ON DELETE CASCADE,
    FOREIGN KEY (user_id) REFERENCES users(id)
);

CREATE TABLE notifications (
    id INT AUTO_INCREMENT PRIMARY KEY,
    user_id INT NOT NULL,
    title VARCHAR(100),
    message TEXT,
    is_read BOOLEAN DEFAULT FALSE,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,

    FOREIGN KEY (user_id)
        REFERENCES users(id)
        ON DELETE CASCADE
);


ALTER TABLE notifications
  ADD COLUMN rental_id INT NULL,
  ADD INDEX idx_notifications_user_read (user_id, is_read),
  ADD INDEX idx_notifications_rental (rental_id);

ALTER TABLE notifications
  ADD UNIQUE KEY uq_notifications_rental_reminder (user_id, rental_id, title);
